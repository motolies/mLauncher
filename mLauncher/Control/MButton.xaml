<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:mLauncher.Control"
             xmlns:mLauncher="clr-namespace:mLauncher" x:Class="mLauncher.Control.MButton"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100" >
    <Button Click="Button_Click" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"  >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="20" />
            </Grid.RowDefinitions>
            <Image Source="{Binding IconImage}" Grid.Row="0" Grid.Column="0" Margin="5" />
            <TextBlock Text="{Binding Text}" Grid.Column="0" Grid.Row="1" TextAlignment="Center" />
        </Grid>
    </Button>

</UserControl>

    